:80 {
    encode gzip

    handle /auth {
        redir /auth/ 308
    }

    handle_path /auth/* {
        reverse_proxy kw_auth_api:8090
    }

    handle {
        root * /srv/web
        try_files {path} /index.html /kw.html
        file_server
    }
}
